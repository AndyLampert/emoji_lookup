
<%= simple_form_for(@post) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <p>Add a comment to: </p><%= f.label :entry %>
    <%#= f.input :post_id %>
    <%= f.input :comments %>
    <%#= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

All comments: <% @post.comments %>
